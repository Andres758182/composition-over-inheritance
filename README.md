# Composition over Inheritance

Employee compensation example implemented in two ways:

- `inheritance/` – inheritance version
- `composition/` – composition version
- `uml/` – UML class diagrams of both versions

## How to run

Requires Python 3. From the repository root:

```bash
python inheritance/main.py
python composition/main.py
```

Both versions print the same results.
